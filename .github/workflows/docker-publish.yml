name: Docker Build & Publish

on:
  push:
    tags:
      - 'v*' 

jobs:
  publish:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: mmosipov2024/tracker

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: mmosipov2024/tracker:latest
          labels: ${{ steps.meta.outputs.labels }}

#Столкнулся с проблемой автопубликации и развертывания в DockerHub.Вроде бы сделал как в примере из предыдущего урока
#ошибка в логах заключается в несоответствии логина и пароля,секреты перенес и все бьется.
#куратор вовремя не ответил, прощу помощи, потому что тут нужно показывать скрин того что сделано
